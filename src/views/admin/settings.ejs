<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - SSIM Admin</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100">
  <div class="flex h-screen">
    <%- include('_sidebar', { currentPage: 'settings', userInfo: userInfo }) %>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col overflow-hidden">
      <header class="bg-white shadow-sm border-b border-gray-200">
        <div class="px-6 py-4">
          <h1 class="text-2xl font-semibold text-gray-800"><%= title %></h1>
        </div>
      </header>

      <main class="flex-1 overflow-y-auto p-6 bg-gray-100">
        <div class="mb-6">
          <p class="text-gray-500">View current configuration settings</p>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
          <!-- Application Settings -->
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
              <svg class="w-5 h-5 mr-2 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
              </svg>
              Application
            </h2>
            <div class="space-y-4">
              <div>
                <label class="block text-sm text-gray-500 mb-1">Base URL</label>
                <div class="bg-gray-50 rounded-lg px-4 py-2 font-mono text-sm text-gray-800">
                  <%= config.appBaseUrl %>
                </div>
              </div>
            </div>
          </div>

          <!-- Payment Settings -->
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
              <svg class="w-5 h-5 mr-2 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"></path>
              </svg>
              Payment (NSIM)
            </h2>
            <div class="space-y-4">
              <div>
                <label class="block text-sm text-gray-500 mb-1">Payment API URL</label>
                <div class="bg-gray-50 rounded-lg px-4 py-2 font-mono text-sm text-gray-800">
                  <%= config.paymentApiUrl || 'Not configured' %>
                </div>
              </div>
              <div>
                <label class="block text-sm text-gray-500 mb-1">Merchant ID</label>
                <div class="bg-gray-50 rounded-lg px-4 py-2 font-mono text-sm text-gray-800">
                  <%= config.merchantId || 'Not configured' %>
                </div>
              </div>
            </div>
          </div>

          <!-- Admin Settings -->
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
              <svg class="w-5 h-5 mr-2 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
              </svg>
              Admin Access
            </h2>
            <div class="space-y-4">
              <div>
                <label class="block text-sm text-gray-500 mb-1">Authorized Emails</label>
                <% if (config.adminEmails && config.adminEmails.length > 0) { %>
                <div class="space-y-2">
                  <% config.adminEmails.forEach(email => { %>
                  <div class="bg-gray-50 rounded-lg px-4 py-2 font-mono text-sm text-gray-800">
                    <%= email %>
                  </div>
                  <% }); %>
                </div>
                <% } else { %>
                <div class="bg-yellow-50 rounded-lg px-4 py-2 text-sm text-yellow-800">
                  <strong>Warning:</strong> No admin emails configured. All authenticated users have admin access.
                </div>
                <% } %>
              </div>
            </div>
          </div>

          <!-- Environment Info -->
          <div class="bg-white rounded-xl shadow-sm p-6">
            <h2 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
              <svg class="w-5 h-5 mr-2 text-gray-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
              </svg>
              Environment
            </h2>
            <div class="space-y-4">
              <div>
                <label class="block text-sm text-gray-500 mb-1">Node Environment</label>
                <div class="bg-gray-50 rounded-lg px-4 py-2 text-sm">
                  <span class="<%= process.env.NODE_ENV === 'production' ? 'text-green-600' : 'text-yellow-600' %> font-medium">
                    <%= process.env.NODE_ENV || 'development' %>
                  </span>
                </div>
              </div>
              <div>
                <label class="block text-sm text-gray-500 mb-1">Trust Proxy</label>
                <div class="bg-gray-50 rounded-lg px-4 py-2 text-sm text-gray-800">
                  <%= process.env.TRUST_PROXY === 'true' ? 'Enabled' : 'Disabled' %>
                </div>
              </div>
            </div>
          </div>
        </div>

        <!-- Info Box -->
        <div class="mt-6 bg-blue-50 rounded-xl p-4 flex items-start">
          <svg class="w-5 h-5 text-blue-600 mt-0.5 mr-3 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <div class="text-sm text-blue-800">
            <p class="font-medium">Configuration is read-only</p>
            <p class="mt-1">Settings are configured via environment variables. Update your <code class="bg-blue-100 px-1 rounded">.env</code> file or container configuration to change these values.</p>
          </div>
        </div>
      </main>
    </div>
  </div>
</body>
</html>
