<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - SSIM Admin</title>
  <link rel="stylesheet" href="/css/tailwind.css">
</head>
<body class="min-h-screen bg-gray-100">
  <div class="flex h-screen">
    <%- include('_sidebar', { currentPage: 'terminals', userInfo: userInfo }) %>

    <!-- Main Content -->
    <div class="flex-1 flex flex-col overflow-hidden">
      <!-- Top Bar -->
      <header class="bg-white shadow-sm border-b border-gray-200">
        <div class="px-6 py-4">
          <div class="flex items-center">
            <a href="/admin/terminals" class="text-gray-500 hover:text-gray-700 mr-4">
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"></path>
              </svg>
            </a>
            <h1 class="text-2xl font-semibold text-gray-800"><%= title %></h1>
          </div>
        </div>
      </header>

      <!-- Content Area -->
      <main class="flex-1 overflow-y-auto p-6 bg-gray-100">
        <div class="max-w-xl mx-auto">
          <div class="bg-white rounded-xl shadow-sm p-6">
            <% if (error) { %>
              <div class="mb-6 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
                <% if (error === 'name_required') { %>
                  Terminal name is required.
                <% } else if (error === 'create_failed') { %>
                  Failed to create terminal. Please try again.
                <% } else { %>
                  An error occurred.
                <% } %>
              </div>
            <% } %>

            <form action="/admin/terminals" method="POST" class="space-y-6">
              <div>
                <label for="name" class="block text-sm font-medium text-gray-700 mb-2">
                  Terminal Name
                </label>
                <input type="text" id="name" name="name" required
                       placeholder="e.g., Register 1, Front Counter"
                       class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500">
                <p class="mt-2 text-sm text-gray-500">
                  Give your terminal a descriptive name to identify it easily.
                </p>
              </div>

              <div class="bg-blue-50 border border-blue-200 rounded-lg p-4">
                <h4 class="text-sm font-medium text-blue-800 mb-2">What happens next?</h4>
                <ol class="text-sm text-blue-700 list-decimal list-inside space-y-1">
                  <li>A 6-digit pairing code will be generated</li>
                  <li>Enter this code on your ESP32 terminal device</li>
                  <li>The terminal will connect and appear as "Online"</li>
                  <li>You can then send QR payments to this terminal</li>
                </ol>
              </div>

              <div class="flex justify-end space-x-4">
                <a href="/admin/terminals"
                   class="px-4 py-2 border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 transition">
                  Cancel
                </a>
                <button type="submit"
                        class="px-6 py-2 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700 transition">
                  Create Terminal
                </button>
              </div>
            </form>
          </div>
        </div>
      </main>
    </div>
  </div>
</body>
</html>
